import { AppLayout } from 'layouts';
import { CharacterEditContainer } from 'pods/character';
import React from 'react';

export const CharacterEditScene = () => (
  <AppLayout>
    <CharacterEditContainer />
  </AppLayout>
);
